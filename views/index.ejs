<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search image</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="icon" type="image/x-icon" href="/images/favicon.png">
    <style>
        body {
            background: #42b883;
        }

        .height {
            height: 100vh;
        }

        .form-container {
            position: relative;
            display: flex;
            align-items: center;
        }

        .form-input {
            height: 55px;
            text-indent: 33px;
            border-radius: 10px;
            padding-right: 50px; /* Space for the button */
        }

        .form-input:focus {
            box-shadow: none;
            border: none;
        }

        .search-button {
            position: absolute;
            right: 10px; /* Position the button inside the input */
            top: 50%;
            transform: translateY(-50%);
            border: none; /* Remove default border */
            background: none; /* Remove default background */
            padding: 0; /* Remove default padding */
            cursor: pointer; /* Change cursor to pointer */
            display: inline-flex; /* Ensure the button is inline with other elements */
            align-items: center; /* Center icon vertically */
            justify-content: center; /* Center icon horizontally */
        }

        .search-button svg {
            width: 21px; /* Set the width of the icon */
            height: 21px; /* Set the height of the icon */
            fill: currentColor; /* Use current text color */
        }

        #loader {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100%;
        }

        body,
        html {
            height: 100%;
        }
    </style>
</head>

<body>
    <div id="search-container" class="container">
        <div class="row height d-flex justify-content-center align-items-center">
            <div class="col-md-6">
                <form action="/search" method="post" id="search-form">
                    <div class="form-container">
                        <input type="text" id="query" name="query" class="form-control form-input"
                            placeholder="Search any images...">
                        <button type="submit" class="search-button">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16">
                                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0" />
                            </svg>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div style="display: none;" id="loader">
        <p class="square-circle-5"></p>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"
        integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script>
        $(document).ready(function () {
            // $("#search-btn").click(function () {
                // $("#search-container").hide();
                // $("#loader").show();
                // var query = $("#query").val()

                // $.ajax({
                //     url: "/search/"+query,
                //     cache: false,
                //     method:"GET",
                //     success: function (html) {
                //         console.log("Hello")
                //     }
                // });
            // });
        });
    </script>
</body>

</html>
