import { BaseModel } from "deepinfra/dist/lib/models/base";
import { EmbeddingsRequest } from "deepinfra/dist/lib/types/embeddings/request";
import { EmbeddingsResponse } from "deepinfra/dist/lib/types/embeddings/response";
import { IClientConfig } from "deepinfra/dist/lib/types/common/client-config";
export declare class Embeddings extends BaseModel {
    constructor(modelName: string, authToken?: string, config?: Partial<IClientConfig>);
    generate(body: EmbeddingsRequest): Promise<EmbeddingsResponse>;
}
